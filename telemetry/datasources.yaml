apiVersion: 1

deleteDatasources:
  - name: Tempo

datasources:

  - name: Tempo
    type: tempo
    access: proxy
    url: ${GF_DATASOURCES_TEMPO_URL}
    basicAuth: false
    isDefault: true
    version: 1
    editable: false
    apiVersion: 1
    uid: tempo
    jsonData:
      httpMethod: GET
      nodeGraph:
        enabled: true